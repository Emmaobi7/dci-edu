# server runtime
NODE_ENV=development
PORT=4000

# postgres (Neon)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DB?sslmode=require

# auth
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d
COOKIE_NAME=wapcharm_token

# cors
CLIENT_ORIGIN=http://localhost:5173
